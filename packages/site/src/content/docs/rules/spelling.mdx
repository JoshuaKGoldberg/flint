---
description: "Spelling linting rules provided by Flint, powered by CSpell."
title: "Spelling Plugin"
---

import { RulesTable } from "~/components/RulesTable.tsx";
import PluginTop from "~/components/PluginTop.astro";

<PluginTop pluginId="spelling" />

See the [CSpell spell checker documentation](https://cspell.org/docs/getting-started) for more details on spell-checking code.

:::note
This plugin requires a **CSpell configuration file** in your repo (for example `cspell.json` or `cspell.config.mjs`).
See [Create a configuration file](https://cspell.org/docs/getting-started#1-create-a-configuration-file).
:::

## Presets

### `logical`

Rules that run CSpell to find likely spell-checking mistakes.

```ts title="flint.config.ts"
import { defineConfig, globs, spelling } from "flint";

export default defineConfig({
	use: [
		{
			files: globs.all,
			rules: spelling.presets.logical,
		},
	],
});
```

## Rules

<RulesTable implementing plugin="spelling" />
