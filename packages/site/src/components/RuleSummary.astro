---
import markdownIt from "markdown-it";
import { getRuleForPlugin } from "./getRuleForPlugin";

interface Props {
	plugin: string;
	rule: string;
}

const renderer = markdownIt();
const rule = getRuleForPlugin(Astro.props.plugin, Astro.props.rule);
---

<blockquote>
	<Fragment set:html={renderer.render(rule.about.description)} />
</blockquote>

<blockquote>
	✅ This rule is included in the{" "}
	<a href={`/rules/${Astro.props.plugin}#${rule.about.preset}`}>
		{Astro.props.plugin}
		<code>{rule.about.preset}</code> preset</a
	>.
</blockquote>
